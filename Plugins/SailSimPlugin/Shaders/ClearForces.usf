
#include "Common.ush"
RWStructuredBuffer<int> Forces : register(u0);

[numthreads(64,1,1)]
void MainCS(uint3 id : SV_DispatchThreadID)
{
    uint i = id.x;
    if(i >= NumVerts*3) return;
    Forces[i] = 0;
}
