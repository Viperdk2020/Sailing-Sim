
#include "/SailSimPlugin/Common.ush"
RWStructuredBuffer<int> Forces:register(u0);
[numthreads(THREAD_GROUP_SIZE,1,1)]
void MainCS(uint3 DTid:SV_DispatchThreadID)
{
    uint idx=DTid.x;
    if(idx>=NumVerts*3) return;
    Forces[idx]=0;
}
